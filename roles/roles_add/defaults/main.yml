---
# defaults for role
roles_add_lab_domain: silvan.uk
roles_add_hostname: gateway
roles_add_fqdn: "{{ roles_add_hostname }}.{{ roles_add_lab_domain }}"

roles_add_server_cert_config:
  # ca_dir: /root/ca
  ca_fqdn: "{{ ca_fqdn }}"
  # ca_secret_passphrase: redhat
  fqdn: "{{ roles_add_fqdn }}"
  cn: "{{ roles_add_fqdn }}"
  subject_alt_name: "IP:{{ site_ip }}"
